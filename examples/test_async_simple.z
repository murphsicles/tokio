// test_async_simple.z — minimal async test
// Tests that async fn body and .await loop work correctly

async fn add_one(x: i64) -> i64 {
    return x + 1;
}

fn main() -> i64 {
    let result = add_one(41).await;
    return result;
}
