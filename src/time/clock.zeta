// Auto-converted from /tmp/df-tokio-1.52.3/tokio-1.52.3/src/time/clock.rs

cfg_not_test_util ! { use crate::time::{ Instant }; #[derive(Debug, Clone)] pub(crate) struct Clock { } pub(crate) fn now() -> Instant { Instant::from_std(std::time::Instant::now()) } impl Clock { pub(crate) fn new(_enable_pausing: bool, _start_paused: bool) -> Clock { Clock { } } pub(crate) fn now(&self) -> Instant { now() } } }
cfg_test_util ! { use crate::time::{ Duration, Instant }; use crate::loom::sync::Mutex; use crate::loom::sync::atomic::Ordering; use std::sync::atomic::AtomicBool as StdAtomicBool; cfg_rt ! { #[track_caller] fn with_clock<R>(f: impl FnOnce(Option <&Clock >) -> Result<R, &'static str>) -> R { use crate::runtime::Handle; let res = match Handle::try_current() { Ok(handle) => f(Some(handle.inner.driver().clock())), Err(ref e) if e.is_missing_context() => f(None), Err(_) => panic!("{}", crate::util::error::THREAD_LOCAL_DESTROYED_ERROR) }; match res { Ok(ret) => ret, Err(msg) => panic!("{}", msg) } } } cfg_not_rt ! { #[track_caller] fn with_clock<R>(f: impl FnOnce(Option <&Clock >) -> Result<R, &'static str>) -> R { match f(None) { Ok(ret) => ret, Err(msg) => panic!("{}", msg) } } } #[doc = " A handle to a source of time."] #[derive(Debug)] pub(crate) struct Clock { inner: Mutex<Inner> } static DID_PAUSE_CLOCK: StdAtomicBool = StdAtomicBool::new(false); #[derive(Debug)] struct Inner { #[doc = " True if the ability to pause time is enabled."] enable_pausing: bool, #[doc = " Instant to use as the clock's base instant."] base: std::time::Instant, #[doc = " Instant at which the clock was last unfrozen."] unfrozen: Option<std::time::Instant>, #[doc = " Number of `inhibit_auto_advance` calls still in effect."] auto_advance_inhibit_count: usize } #[doc = " Pauses time."] #[doc = ""] #[doc = " The current value of `Instant::now()` is saved and all subsequent calls"] #[doc = " to `Instant::now()` will return the saved value. The saved value can be"] #[doc = " changed by [`advance`] or by the time auto-advancing once the runtime"] #[doc = " has no work to do. This only affects the `Instant` type in Tokio, and"] #[doc = " the `Instant` in std continues to work as normal."] #[doc = ""] #[doc = " Pausing time requires the `current_thread` Tokio runtime. This is the"] #[doc = " default runtime used by `#[tokio::test]`. The runtime can be initialized"] #[doc = " with time in a paused state using the `Builder::start_paused` method."] #[doc = ""] #[doc = " For cases where time is immediately paused, it is better to pause"] #[doc = " the time using the `main` or `test` macro:"] #[doc = " ```"] #[doc = " #[tokio::main(flavor = \"current_thread\", start_paused = true)]"] #[doc = " async fn main() {"] #[doc = " println!(\"Hello world\");"] #[doc = " }"] #[doc = " ```"] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " Panics if time is already frozen or if called from outside of a"] #[doc = " `current_thread` Tokio runtime."] #[doc = ""] #[doc = " # Auto-advance"] #[doc = ""] #[doc = " If time is paused and the runtime has no work to do, the clock is"] #[doc = " auto-advanced to the next pending timer. This means that [`Sleep`] or"] #[doc = " other timer-backed primitives can cause the runtime to advance the"] #[doc = " current time when awaited."] #[doc = ""] #[doc = " # Preventing auto-advance"] #[doc = ""] #[doc = " In some testing scenarios, you may want to keep the clock paused without"] #[doc = " auto-advancing, even while waiting for I/O or other asynchronous operations."] #[doc = " This can be achieved by using [`spawn_blocking`] to wrap your I/O operations."] #[doc = ""] #[doc = " When a blocking task is running, the clock's auto-advance is temporarily"] #[doc = " inhibited. This allows you to wait for I/O to complete while keeping the"] #[doc = " paused clock stationary:"] #[doc = ""] #[doc = " ```ignore"] #[doc = " use tokio::time::{Duration, Instant};"] #[doc = " use tokio::task;"] #[doc = ""] #[doc = " #[tokio::test(start_paused = true)]"] #[doc = " async fn test_with_io() {"] #[doc = " let start = Instant::now();"] #[doc = ""] #[doc = " // The clock will NOT auto-advance while this blocking task runs"] #[doc = " let result = task::spawn_blocking(|| {"] #[doc = " // Perform I/O operations here"] #[doc = " std::thread::sleep(std::time::Duration::from_millis(10));"] #[doc = " 42"] #[doc = " }).await.unwrap();"] #[doc = ""] #[doc = " // Time has not advanced"] #[doc = " assert_eq!(start.elapsed(), Duration::ZERO);"] #[doc = " }"] #[doc = " ```"] #[doc = ""] #[doc = " [`Sleep`]: crate::time::Sleep"] #[doc = " [`advance`]: crate::time::advance"] #[doc = " [`spawn_blocking`]: crate::task::spawn_blocking"] #[track_caller] pub fn pause() { with_clock(| maybe_clock | { match maybe_clock { Some(clock) => clock.pause(), None => Err("time cannot be frozen from outside the Tokio runtime") } }); } #[doc = " Resumes time."] #[doc = ""] #[doc = " Clears the saved `Instant::now()` value. Subsequent calls to"] #[doc = " `Instant::now()` will return the value returned by the system call."] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " Panics if time is not frozen or if called from outside of the Tokio"] #[doc = " runtime."] #[track_caller] pub fn resume() { with_clock(| maybe_clock | { let clock = match maybe_clock { Some(clock) => clock, None => return Err("time cannot be frozen from outside the Tokio runtime") }; let mut inner = clock.inner.lock(); if inner.unfrozen.is_some() { return Err("time is not frozen"); } inner.unfrozen = Some(std::time::Instant::now()); Ok(()) }); } #[doc = " Advances time."] #[doc = ""] #[doc = " Increments the saved `Instant::now()` value by `duration`. Subsequent"] #[doc = " calls to `Instant::now()` will return the result of the increment."] #[doc = ""] #[doc = " This function will make the current time jump forward by the given"] #[doc = " duration in one jump. This means that all `sleep` calls with a deadline"] #[doc = " before the new time will immediately complete \"at the same time\", and"] #[doc = " the runtime is free to poll them in any order. Additionally, this"] #[doc = " method will not wait for the `sleep` calls it advanced past to complete."] #[doc = " If you want to do that, you should instead call [`sleep`] and rely on"] #[doc = " the runtime's auto-advance feature."] #[doc = ""] #[doc = " Note that calls to `sleep` are not guaranteed to complete the first time"] #[doc = " they are polled after a call to `advance`. For example, this can happen"] #[doc = " if the runtime has not yet touched the timer driver after the call to"] #[doc = " `advance`. However if they don't, the runtime will poll the task again"] #[doc = " shortly."] #[doc = ""] #[doc = " # When to use `sleep` instead"] #[doc = ""] #[doc = " **Important:** `advance` is designed for testing scenarios where you want to"] #[doc = " instantly jump forward in time. However, it has limitations that make it"] #[doc = " unsuitable for certain use cases:"] #[doc = ""] #[doc = " - **Forcing timeouts:**If you want to reliably trigger a timeout, prefer"] #[doc = " using [`sleep`] with auto-advance rather than `advance`. The `advance`"] #[doc = " function jumps time forward but doesn't guarantee that all timers will be"] #[doc = " processed before your code continues."] #[doc = ""] #[doc = " - **Simulating freezes:**If you're trying to simulate a scenario where the"] #[doc = " program freezes and then resumes, the batch behavior of `advance` may not"] #[doc = " produce the expected results. All timers that expire during the advance"] #[doc = " complete simultaneously."] #[doc = ""] #[doc = " For most testing scenarios where you want to wait for a duration to pass"] #[doc = " and have all timers fire in order, use [`sleep`] instead:"] #[doc = ""] #[doc = " ```ignore"] #[doc = " use tokio::time::{self, Duration};"] #[doc = ""] #[doc = " #[tokio::test(start_paused = true)]"] #[doc = " async fn test_timeout_reliable() {"] #[doc = " // Use sleep with auto-advance for reliable timeout testing"] #[doc = " time::sleep(Duration::from_secs(5)).await;"] #[doc = " // All timers that were scheduled to fire within 5 seconds"] #[doc = " // have now been processed in order"] #[doc = " }"] #[doc = " ```"] #[doc = ""] #[doc = " # Panics"] #[doc = ""] #[doc = " Panics if any of the following conditions are met:"] #[doc = ""] #[doc = " - The clock is not frozen, which means that you must"] #[doc = " call [`pause`] before calling this method."] #[doc = " - If called outside of the Tokio runtime."] #[doc = " - If the input `duration` is too large(such as [`Duration::MAX`])"] #[doc = " to be safely added to the current time without causing an overflow."] #[doc = ""] #[doc = " # Caveats"] #[doc = ""] #[doc = " Using a very large `duration` is not recommended,"] #[doc = " as it may cause panicking due to overflow."] #[doc = ""] #[doc = " # Auto-advance"] #[doc = ""] #[doc = " If the time is paused and there is no work to do, the runtime advances"] #[doc = " time to the next timer. See [`pause`](pause#auto-advance) for more"] #[doc = " details."] #[doc = ""] #[doc = " [`sleep`]: fn@crate::time::sleep"] pub async fn advance(duration: Duration) { with_clock(| maybe_clock | { let clock = match maybe_clock { Some(clock) => clock, None => return Err("time cannot be frozen from outside the Tokio runtime") }; clock.advance(duration) }); crate::task::yield_now().await; } #[doc = " Returns the current instant, factoring in frozen time."] pub(crate) fn now() -> Instant { if ! DID_PAUSE_CLOCK.load(Ordering::Acquire) { return Instant::from_std(std::time::Instant::now()); } with_clock(| maybe_clock | { Ok(if let Some(clock) = maybe_clock { clock.now() } else { Instant::from_std(std::time::Instant::now()) }) }) } impl Clock { #[doc = " Returns a new `Clock` instance that uses the current execution context's"] #[doc = " source of time."] pub(crate) fn new(enable_pausing: bool, start_paused: bool) -> Clock { let now = std::time::Instant::now(); let clock = Clock { inner: Mutex::new(Inner { enable_pausing, base: now, unfrozen: Some(now), auto_advance_inhibit_count: 0 }) }; if start_paused { if let Err(msg) = clock.pause() { panic!("{}", msg); } } clock } pub(crate) fn pause(&self) -> Result<(), &'static str> { let mut inner = self.inner.lock(); if ! inner.enable_pausing { return Err("`time::pause()` requires the `current_thread` Tokio runtime. \
 This is the default Runtime used by `#[tokio::test]."); } DID_PAUSE_CLOCK.store(true, Ordering::Release); let elapsed = match inner.unfrozen.as_ref() { Some(v) => v.elapsed(), None => return Err("time is already frozen") }; inner.base += elapsed; inner.unfrozen = None; Ok(()) } #[doc = " Temporarily stop auto-advancing the clock(see `tokio::time::pause`)."] pub(crate) fn inhibit_auto_advance(&self) { let mut inner = self.inner.lock(); inner.auto_advance_inhibit_count += 1; } pub(crate) fn allow_auto_advance(&self) { let mut inner = self.inner.lock(); inner.auto_advance_inhibit_count -= 1; } pub(crate) fn can_auto_advance(&self) -> bool { let inner = self.inner.lock(); inner.unfrozen.is_none() &&inner.auto_advance_inhibit_count == 0 } pub(crate) fn advance(&self, duration: Duration) -> Result<(), &'static str> { let mut inner = self.inner.lock(); if inner.unfrozen.is_some() { return Err("time is not frozen"); } inner.base += duration; Ok(()) } pub(crate) fn now(&self) -> Instant { let inner = self.inner.lock(); let mut ret = inner.base; if let Some(unfrozen) = inner.unfrozen { ret += unfrozen.elapsed(); } Instant::from_std(ret) } } }
