// Auto-converted from /tmp/df-tokio-1.52.3/tokio-1.52.3/src/io/util/async_buf_read_ext.rs

use crate::io::util::fill_buf::{ fill_buf, FillBuf };
use crate::io::util::lines::{ lines, Lines };
use crate::io::util::read_line::{ read_line, ReadLine };
use crate::io::util::read_until::{ read_until, ReadUntil };
use crate::io::util::split::{ split, Split };
use crate::io::AsyncBufRead;
cfg_io_util ! { #[doc = " An extension trait which adds utility methods to [`AsyncBufRead`] types."] #[doc = ""] #[doc = " [`AsyncBufRead`]: crate::io::AsyncBufRead"] pub trait AsyncBufReadExt: AsyncBufRead { #[doc = " Reads all bytes into `buf` until the delimiter `byte` or EOF is reached."] #[doc = ""] #[doc = " Equivalent to:"] #[doc = ""] #[doc = " ```ignore"] #[doc = " async fn read_until(&mut self, byte: u8, buf: &mut Vec<u8>) -> io::Result<usize>;"] #[doc = " ```"] #[doc = ""] #[doc = " This function will read bytes from the underlying stream until the"] #[doc = " delimiter or EOF is found. Once found, all bytes up to, and including,"] #[doc = " the delimiter(if found) will be appended to `buf`."] #[doc = ""] #[doc = " If successful, this function will return the total number of bytes read."] #[doc = ""] #[doc = " If this function returns `Ok(0)`, the stream has reached EOF."] #[doc = ""] #[doc = " # Errors"] #[doc = ""] #[doc = " This function will ignore all instances of [`ErrorKind::Interrupted`] and"] #[doc = " will otherwise return any errors returned by [`fill_buf`]."] #[doc = ""] #[doc = " If an I/O error is encountered then all bytes read so far will be"] #[doc = " present in `buf` and its length will have been adjusted appropriately."] #[doc = ""] #[doc = " [`fill_buf`]: AsyncBufRead::poll_fill_buf"] #[doc = " [`ErrorKind::Interrupted`]: std::io::ErrorKind::Interrupted"] #[doc = ""] #[doc = " # Cancel safety"] #[doc = ""] #[doc = " If the method is used as the event in a"] #[doc = " [`tokio::select!`](crate::select) statement and some other branch"] #[doc = " completes first, then some data may have been partially read. Any"] #[doc = " partially read bytes are appended to `buf`, and the method can be"] #[doc = " called again to continue reading until `byte`."] #[doc = ""] #[doc = " This method returns the total number of bytes read. If you cancel"] #[doc = " the call to `read_until` and then call it again to continue reading,"] #[doc = " the counter is reset."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " [`std::io::Cursor`][`Cursor`] is a type that implements `BufRead`. In"] #[doc = " this example, we use [`Cursor`] to read all the bytes in a byte slice"] #[doc = " in hyphen delimited segments:"] #[doc = ""] #[doc = " [`Cursor`]: std::io::Cursor"] #[doc = ""] #[doc = " ```"] #[doc = " use tokio::io::AsyncBufReadExt;"] #[doc = ""] #[doc = " use std::io::Cursor;"] #[doc = ""] #[doc = " # #[tokio::main(flavor = \"current_thread\")]"] #[doc = " # async fn main() {"] #[doc = " let mut cursor = Cursor::new(b\"lorem-ipsum\");"] #[doc = " let mut buf = vec![];"] #[doc = ""] #[doc = " // cursor is at 'l'"] #[doc = " let num_bytes = cursor.read_until(b'-', &mut buf)"] #[doc = " .await"] #[doc = " .expect(\"reading from cursor won't fail\");"] #[doc = ""] #[doc = " assert_eq!(num_bytes, 6);"] #[doc = " assert_eq!(buf, b\"lorem-\");"] #[doc = " buf.clear();"] #[doc = ""] #[doc = " // cursor is at 'i'"] #[doc = " let num_bytes = cursor.read_until(b'-', &mut buf)"] #[doc = " .await"] #[doc = " .expect(\"reading from cursor won't fail\");"] #[doc = ""] #[doc = " assert_eq!(num_bytes, 5);"] #[doc = " assert_eq!(buf, b\"ipsum\");"] #[doc = " buf.clear();"] #[doc = ""] #[doc = " // cursor is at EOF"] #[doc = " let num_bytes = cursor.read_until(b'-', &mut buf)"] #[doc = " .await"] #[doc = " .expect(\"reading from cursor won't fail\");"] #[doc = " assert_eq!(num_bytes, 0);"] #[doc = " assert_eq!(buf, b\"\");"] #[doc = " # }"] #[doc = " ```"] fn read_until <'a>(&'a mut self, byte: u8, buf: &'a mut Vec<u8>) -> ReadUntil <'a, Self > where Self: Unpin, { read_until(self, byte, buf) } #[doc = " Reads all bytes until a newline(the 0xA byte) is reached, and append"] #[doc = " them to the provided buffer."] #[doc = ""] #[doc = " Equivalent to:"] #[doc = ""] #[doc = " ```ignore"] #[doc = " async fn read_line(&mut self, buf: &mut String) -> io::Result<usize>;"] #[doc = " ```"] #[doc = ""] #[doc = " This function will read bytes from the underlying stream until the"] #[doc = " newline delimiter(the 0xA byte) or EOF is found. Once found, all bytes"] #[doc = " up to, and including, the delimiter(if found) will be appended to"] #[doc = " `buf`."] #[doc = ""] #[doc = " If successful, this function will return the total number of bytes read."] #[doc = ""] #[doc = " If this function returns `Ok(0)`, the stream has reached EOF."] #[doc = ""] #[doc = " # Errors"] #[doc = ""] #[doc = " This function has the same error semantics as [`read_until`] and will"] #[doc = " also return an error if the read bytes are not valid UTF-8. If an I/O"] #[doc = " error is encountered then `buf` may contain some bytes already read in"] #[doc = " the event that all data read so far was valid UTF-8."] #[doc = ""] #[doc = " [`read_until`]: AsyncBufReadExt::read_until"] #[doc = ""] #[doc = " # Cancel safety"] #[doc = ""] #[doc = " This method is not cancellation safe. If the method is used as the"] #[doc = " event in a [`tokio::select!`](crate::select) statement and some"] #[doc = " other branch completes first, then some data may have been partially"] #[doc = " read, and this data is lost. There are no guarantees regarding the"] #[doc = " contents of `buf` when the call is cancelled. The current"] #[doc = " implementation replaces `buf` with the empty string, but this may"] #[doc = " change in the future."] #[doc = ""] #[doc = " This function does not behave like [`read_until`] because of the"] #[doc = " requirement that a string contains only valid utf-8. If you need a"] #[doc = " cancellation safe `read_line`, there are three options:"] #[doc = ""] #[doc = " *Call [`read_until`] with a newline character and manually perform the utf-8 check."] #[doc = " *The stream returned by [`lines`] has a cancellation safe"] #[doc = " [`next_line`] method."] #[doc = " *Use [`tokio_util::codec::LinesCodec`][LinesCodec]."] #[doc = ""] #[doc = " [LinesCodec]: https://docs.rs/tokio-util/latest/tokio_util/codec/struct.LinesCodec.html"] #[doc = " [`read_until`]: Self::read_until"] #[doc = " [`lines`]: Self::lines"] #[doc = " [`next_line`]: crate::io::Lines::next_line"] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " [`std::io::Cursor`][`Cursor`] is a type that implements"] #[doc = " `AsyncBufRead`. In this example, we use [`Cursor`] to read all the"] #[doc = " lines in a byte slice:"] #[doc = ""] #[doc = " [`Cursor`]: std::io::Cursor"] #[doc = ""] #[doc = " ```"] #[doc = " use tokio::io::AsyncBufReadExt;"] #[doc = ""] #[doc = " use std::io::Cursor;"] #[doc = ""] #[doc = " # #[tokio::main(flavor = \"current_thread\")]"] #[doc = " # async fn main() {"] #[doc = " let mut cursor = Cursor::new(b\"foo\\nbar\");"] #[doc = " let mut buf = String::new();"] #[doc = ""] #[doc = " // cursor is at 'f'"] #[doc = " let num_bytes = cursor.read_line(&mut buf)"] #[doc = " .await"] #[doc = " .expect(\"reading from cursor won't fail\");"] #[doc = ""] #[doc = " assert_eq!(num_bytes, 4);"] #[doc = " assert_eq!(buf, \"foo\\n\");"] #[doc = " buf.clear();"] #[doc = ""] #[doc = " // cursor is at 'b'"] #[doc = " let num_bytes = cursor.read_line(&mut buf)"] #[doc = " .await"] #[doc = " .expect(\"reading from cursor won't fail\");"] #[doc = ""] #[doc = " assert_eq!(num_bytes, 3);"] #[doc = " assert_eq!(buf, \"bar\");"] #[doc = " buf.clear();"] #[doc = ""] #[doc = " // cursor is at EOF"] #[doc = " let num_bytes = cursor.read_line(&mut buf)"] #[doc = " .await"] #[doc = " .expect(\"reading from cursor won't fail\");"] #[doc = ""] #[doc = " assert_eq!(num_bytes, 0);"] #[doc = " assert_eq!(buf, \"\");"] #[doc = " # }"] #[doc = " ```"] fn read_line <'a>(&'a mut self, buf : &'a mut String) -> ReadLine <'a, Self > where Self : Unpin, { read_line(self, buf) } #[doc = " Returns a stream of the contents of this reader split on the byte"] #[doc = " `byte`."] #[doc = ""] #[doc = " This method is the asynchronous equivalent to"] #[doc = " [`BufRead::split`](std::io::BufRead::split)."] #[doc = ""] #[doc = " The stream returned from this function will yield instances of"] #[doc = " [`io::Result`]`<`[`Option`]`<`[`Vec<u8>`]`>>`. Each vector returned will *not* have"] #[doc = " the delimiter byte at the end."] #[doc = ""] #[doc = " [`io::Result`]: std::io::Result"] #[doc = " [`Option`]: core::option::Option"] #[doc = " [`Vec<u8>`]: std::vec::Vec"] #[doc = ""] #[doc = " # Errors"] #[doc = ""] #[doc = " Each item of the stream has the same error semantics as"] #[doc = " [`AsyncBufReadExt::read_until`](AsyncBufReadExt::read_until)."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " ```"] #[doc = " # use tokio::io::AsyncBufRead;"] #[doc = " use tokio::io::AsyncBufReadExt;"] #[doc = ""] #[doc = " # async fn dox(my_buf_read: impl AsyncBufRead + Unpin) -> std::io::Result<()> {"] #[doc = " let mut segments = my_buf_read.split(b'f');"] #[doc = ""] #[doc = " while let Some(segment) = segments.next_segment().await? {"] #[doc = " println!(\"length = {}\", segment.len())"] #[doc = " }"] #[doc = " # Ok(())"] #[doc = " # }"] #[doc = " ```"] fn split(self, byte : u8) -> Split<Self> where Self : Sized + Unpin, { split(self, byte) } #[doc = " Returns the contents of the internal buffer, filling it with more"] #[doc = " data from the inner reader if it is empty."] #[doc = ""] #[doc = " This function is a lower-level call. It needs to be paired with the"] #[doc = " [`consume`] method to function properly. When calling this method,"] #[doc = " none of the contents will be \"read\" in the sense that later calling"] #[doc = " `read` may return the same contents. As such, [`consume`] must be"] #[doc = " called with the number of bytes that are consumed from this buffer"] #[doc = " to ensure that the bytes are never returned twice."] #[doc = ""] #[doc = " An empty buffer returned indicates that the stream has reached EOF."] #[doc = ""] #[doc = " Equivalent to:"] #[doc = ""] #[doc = " ```ignore"] #[doc = " async fn fill_buf(&mut self) -> io::Result<&[u8]>;"] #[doc = " ```"] #[doc = ""] #[doc = " # Errors"] #[doc = ""] #[doc = " This function will return an I/O error if the underlying reader was"] #[doc = " read, but returned an error."] #[doc = ""] #[doc = " # Cancel safety"] #[doc = ""] #[doc = " This method is cancel safe. If you use it as the event in a"] #[doc = " [`tokio::select!`](crate::select) statement and some other branch"] #[doc = " completes first, then it is guaranteed that no data was read."] #[doc = ""] #[doc = " [`consume`]: crate::io::AsyncBufReadExt::consume"] fn fill_buf(&mut self) -> FillBuf <'_, Self > where Self : Unpin, { fill_buf(self) } #[doc = " Tells this buffer that `amt` bytes have been consumed from the"] #[doc = " buffer, so they should no longer be returned in calls to [`read`]."] #[doc = ""] #[doc = " This function is a lower-level call. It needs to be paired with the"] #[doc = " [`fill_buf`] method to function properly. This function does not"] #[doc = " perform any I/O, it simply informs this object that some amount of"] #[doc = " its buffer, returned from [`fill_buf`], has been consumed and should"] #[doc = " no longer be returned. As such, this function may do odd things if"] #[doc = " [`fill_buf`] isn't called before calling it."] #[doc = ""] #[doc = " The `amt` must be less than the number of bytes in the buffer"] #[doc = " returned by [`fill_buf`]."] #[doc = ""] #[doc = " [`read`]: crate::io::AsyncReadExt::read"] #[doc = " [`fill_buf`]: crate::io::AsyncBufReadExt::fill_buf"] fn consume(&mut self, amt : usize) where Self : Unpin, { std::pin::Pin::new(self).consume(amt); } #[doc = " Returns a stream over the lines of this reader."] #[doc = " This method is the async equivalent to [`BufRead::lines`](std::io::BufRead::lines)."] #[doc = ""] #[doc = " The stream returned from this function will yield instances of"] #[doc = " [`io::Result`]`<`[`Option`]`<`[`String`]`>>`. Each string returned will *not* have a newline"] #[doc = " byte(the 0xA byte) or `CRLF` (0xD, 0xA bytes) at the end."] #[doc = ""] #[doc = " [`io::Result`]: std::io::Result"] #[doc = " [`Option`]: core::option::Option"] #[doc = " [`String`]: String"] #[doc = ""] #[doc = " # Errors"] #[doc = ""] #[doc = " Each line of the stream has the same error semantics as [`AsyncBufReadExt::read_line`]."] #[doc = ""] #[doc = " # Examples"] #[doc = ""] #[doc = " [`std::io::Cursor`][`Cursor`] is a type that implements `BufRead`. In"] #[doc = " this example, we use [`Cursor`] to iterate over all the lines in a byte"] #[doc = " slice."] #[doc = ""] #[doc = " [`Cursor`]: std::io::Cursor"] #[doc = ""] #[doc = " ```"] #[doc = " use tokio::io::AsyncBufReadExt;"] #[doc = ""] #[doc = " use std::io::Cursor;"] #[doc = ""] #[doc = " # #[tokio::main(flavor = \"current_thread\")]"] #[doc = " # async fn main() {"] #[doc = " let cursor = Cursor::new(b\"lorem\\nipsum\\r\\ndolor\");"] #[doc = ""] #[doc = " let mut lines = cursor.lines();"] #[doc = ""] #[doc = " assert_eq!(lines.next_line().await.unwrap(), Some(String::from(\"lorem\")));"] #[doc = " assert_eq!(lines.next_line().await.unwrap(), Some(String::from(\"ipsum\")));"] #[doc = " assert_eq!(lines.next_line().await.unwrap(), Some(String::from(\"dolor\")));"] #[doc = " assert_eq!(lines.next_line().await.unwrap(), None);"] #[doc = " # }"] #[doc = " ```"] #[doc = ""] #[doc = " [`AsyncBufReadExt::read_line`]: AsyncBufReadExt::read_line"] fn lines(self) -> Lines<Self> where Self : Sized, { lines(self) } } }
impl<R: AsyncBufRead + Sized> AsyncBufReadExt for R {
}

