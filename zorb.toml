[package]
name = "@async/tokio"
version = "1.52.3"
description = "Full async runtime for Zeta — multi-thread scheduler, TCP/UDP/Unix net, filesystem, sync primitives, signals, time"
license = "MIT"
repository = "https://github.com/murphsicles/tokio"
readme = "README.md"

[fmt]
style = "zeta-strict"
